import 'package:flutter/material.dart';

class DonarDetailsPage extends StatelessWidget {
  static const String routeName = '/Donordetails';
  const DonarDetailsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
